/**
*	Given one or two string, check if either are a potential PO Box
*
*   @input Address1 : String First address to check
*	@input Address2 : String Second address to check
*
*/
importPackage( dw.system );
importScript( "int_shoprunner:checkout/Utils.ds" );

function execute( args : PipelineDictionary ) : Number
{	
    return PIPELET_NEXT;
}
