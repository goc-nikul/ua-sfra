<iscontent type="text/html" charset="UTF-8" compact="true"/>
<iscomponent pipeline="CM-RemoteFragment"
             pageId="${pdict.pageid}"
             productId="${pdict.productid}"
             categoryId="${pdict.categoryid}"
             placement="${pdict.placement}"
             view="${pdict.view}"
             externalRef="${pdict.externalref}"
             parameter="${pdict.parameter}"
             preview="${pdict.CurrentHttpParameterMap.preview.stringValue}"/>
