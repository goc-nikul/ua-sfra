<iscontent type="text/html" charset="UTF-8" compact="true"/>
<isscript>
    var CM = require('~/cartridge/controllers/CM');
    let fragment = CM.Fragment(pdict, request, response, false);
</isscript>
<isset name="fragmentPayload" value="${fragment.payload}" scope="page"/>
<isset name="fragmentHttpStatus" value="${fragment.status}" scope="page"/>