<div>
    <div id="wishlistItemCards">
        <isif condition="${pdict.wishlist && pdict.wishlist.items.length > 0 && (!pdict.listIsEmpty || pdict.listIsEmpty == null ) }">
            <isinclude template="wishlist/wishlistSortOrder" />
        </isif>
        <isinclude template="wishlist/components/list">
    </div>
</div>
