<iscomment> Shop All[category] is only display for mobile view</iscomment>
<isif condition="${menuItem.displayShopAllLinkInMobile}"> 
    <a class="b-navigation-title_link js-menu-title b-shopall_link" href="${menuItem.url}" title="${menuItem.name}" data-shopall-attr = 'isShopAllUrl' data-menu-title="${menuItem.name}"
            data-analytics-nav-track="${uaAnalyticsPathTop}|${menuItem.url}">
            <isprint value="${Resource.msgf('label.shop.all', 'common', null, menuItem.name)}" />
     </a>
</isif>
