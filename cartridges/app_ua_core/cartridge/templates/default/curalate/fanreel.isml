<!--- TEMPLATENAME: funreel.isml --->

<div data-crl8-container-id="${pdict.container}" <isif condition="${!empty(pdict.filter)}">data-crl8-filter="${pdict.filter}"</isif> <isif condition="${!empty(pdict.locale)}">data-crl8-api-locale="${pdict.locale}"</isif>></div>