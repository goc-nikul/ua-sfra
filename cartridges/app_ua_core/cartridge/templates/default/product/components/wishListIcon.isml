<button class="b-tile-fav_defultButton wishlistTile product"
    remove-href="${URLUtils.url('Wishlist-RemoveProduct')}" href="${URLUtils.url('Wishlist-AddProduct')}"
    aria-label="${Resource.msg('button.addto.wishlist', 'common', null)}" data-pid="${pdict.mid}"
    type="button" data-analytics-style="${(pdict.productStyle)? pdict.productStyle : ''}">
    <span class=" js-whislist-icon ${pdict.isItemExistsInWishList ? 'product-added b-tile-fav_selectButton' : 'b-tile-fav_button'}">
    </span>
</button>