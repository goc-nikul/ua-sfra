/**
 * Parses the keywords from a product.
 *
 * @param {dw.catalog.Product} product The product.
 * @returns {string[]} The keywords.
 */
module.exports = function getKeywords(_product) {
  return [];
};
